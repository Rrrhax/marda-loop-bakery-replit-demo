# Marda Loop Bakery Replit Demo

1. Add secrets: TELEGRAM_TOKEN = your bot token
2. Run → copy Webview URL (e.g. https://your-repl.repl.co) to WEBAPP_URL secret
3. Shell: python bot.py
4. TG /start @StoreDemoForBot → test order.